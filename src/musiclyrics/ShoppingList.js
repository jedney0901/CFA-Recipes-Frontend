import React, { Component } from 'react';
import axios from 'axios';

class ShoppingList extends Component {

  getMusicLyrics () {
    const MUSICAPI = 'https://'

    axios.get(MUSICAPI) =
      .then((response) => {

      })
  }
}
